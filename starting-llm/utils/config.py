# Config"""
Hyperparameters and configuration
"""

# TODO:
# [ ] Define model hyperparameters (d_model, n_layers, n_heads, etc.)
# [ ] Define training hyperparameters (lr, batch_size, epochs, etc.)
# [ ] Define data hyperparameters (context_length, vocab_size, etc.)
# [ ] Add config loading from YAML/JSON
# [ ] Add config validation
# [ ] Support different model sizes (small, medium, large)
